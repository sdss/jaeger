Configuration file
==================

jaeger uses an internal YAML file to store configuration values that may be
overridden by the user as described below. The default configuration as of
version |jaeger_version| is:

.. literalinclude:: ../../python/jaeger/etc/jaeger.yml
   :language: yaml
