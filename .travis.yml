cache:
  pip: true
  timeout: 1000

language: python

matrix:
  fast_finish: true
  include:
    - python: 3.7
      dist: xenial
      sudo: true
      after_success:
        - coveralls
    - python: 3.8
      dist: xenial
      sudo: true

notifications:
  email: false

# repo branches to test
branches:
  - master
  - build-cleanup

before_install:
  - pip install poetry

install:
  - poetry install

script:
  - pytest --cov jaeger --cov-report html
